---
- name: Prepare containers for system_prep
  hosts: all
  gather_facts: yes
  tasks:
    - name: Set besu_node_ip for template compatibility
      set_fact:
        besu_node_ip: "{{ '10.10.20.11' if inventory_hostname == 'bootnode-1' else '10.10.20.13' }}"
